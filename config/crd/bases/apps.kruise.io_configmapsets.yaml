---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: configmapsets.apps.kruise.io
spec:
  group: apps.kruise.io
  names:
    kind: ConfigMapSet
    listKind: ConfigMapSetList
    plural: configmapsets
    singular: configmapset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The number of pods matched.
      jsonPath: .status.replicas
      name: REPLICAS
      type: integer
    - description: The number of pods matched and updated.
      jsonPath: .status.updatedReplicas
      name: UPDATED
      type: integer
    - description: The number of pods matched and ready.
      jsonPath: .status.readyReplicas
      name: READY
      type: integer
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created.
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigMapSet is the Schema for the configmapsets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec represents the desired behavior of the ConfigMapSet.
            properties:
              containers:
                description: |-
                  Containers defines the list of business containers within the matched Pods
                  that need to mount the configuration data.
                items:
                  description: ConfigMapSetContainer specifies a target container
                    and where the configuration should be mounted.
                  properties:
                    mountPath:
                      description: MountPath is the directory path inside the container
                        where the configuration files will be mounted.
                      type: string
                    name:
                      description: Name of the target container to mount the configuration.
                      type: string
                    nameFrom:
                      description: |-
                        NameFrom provides a dynamic way to determine the container name (e.g., via Downward API)
                        to support dynamically injected containers like SidecarSets.
                      properties:
                        fieldRef:
                          description: 'Selects a field of the pod: supports metadata.name,
                            `metadata.labels[''<KEY>'']`, `metadata.annotations[''<KEY>'']`,'
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  type: object
                type: array
              customVersion:
                description: |-
                  CustomVersion is an optional string to provide a user-defined version or alias
                  for the current configuration data. It can be used for version tracking.
                type: string
              data:
                additionalProperties:
                  type: string
                description: |-
                  Data contains the actual configuration files (key-value pairs) to be distributed
                  to the matched Pods. Keys are file names, and values are the file contents.
                type: object
              effectPolicy:
                description: |-
                  EffectPolicy defines the strategy for how configuration updates should be applied
                  to the Pods (e.g., restarting containers, hot updating, or triggering webhooks).
                properties:
                  postHook:
                    description: |-
                      PostHook defines the callback actions to trigger business containers to reload configurations
                      when Type is "PostHook".
                    properties:
                      httpGet:
                        description: HTTPGet defines an HTTP GET request to perform
                          as a post-hook action.
                        items:
                          description: HTTPGetAction describes an action based on
                            HTTP Get requests.
                          properties:
                            host:
                              description: |-
                                Host name to connect to, defaults to the pod IP. You probably want to set
                                "Host" in httpHeaders instead.
                              type: string
                            httpHeaders:
                              description: Custom headers to set in the request. HTTP
                                allows repeated headers.
                              items:
                                description: HTTPHeader describes a custom header
                                  to be used in HTTP probes
                                properties:
                                  name:
                                    description: |-
                                      The header field name.
                                      This will be canonicalized upon output, so case-variant names will be understood as the same header.
                                    type: string
                                  value:
                                    description: The header field value
                                    type: string
                                required:
                                - name
                                - value
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            path:
                              description: Path to access on the HTTP server.
                              type: string
                            port:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Name or number of the port to access on the container.
                                Number must be in the range 1 to 65535.
                                Name must be an IANA_SVC_NAME.
                              x-kubernetes-int-or-string: true
                            scheme:
                              description: |-
                                Scheme to use for connecting to the host.
                                Defaults to HTTP.
                              type: string
                          required:
                          - port
                          type: object
                        type: array
                      tcpSocket:
                        description: TCPSocket defines a TCP socket connection to
                          perform as a post-hook action.
                        items:
                          description: TCPSocketAction describes an action based on
                            opening a socket
                          properties:
                            host:
                              description: 'Optional: Host name to connect to, defaults
                                to the pod IP.'
                              type: string
                            port:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                Number or name of the port to access on the container.
                                Number must be in the range 1 to 65535.
                                Name must be an IANA_SVC_NAME.
                              x-kubernetes-int-or-string: true
                          required:
                          - port
                          type: object
                        type: array
                    type: object
                  type:
                    description: |-
                      Type indicates the method of applying updates.
                      "Restart": Restarts the reload-sidecar first, then restarts business containers.
                      "PostHook": Restarts the reload-sidecar, then triggers HTTP/TCP callbacks to business containers.
                      "HotUpdate": Neither container restarts; reload-sidecar updates shared files and business containers hot-reload.
                    type: string
                type: object
              reloadSidecarConfig:
                description: |-
                  ReloadSidecarConfig configures the sidecar container responsible for dynamically
                  receiving configuration updates and sharing them with the business containers.
                properties:
                  config:
                    description: Config provides the detailed configuration parameters
                      for the chosen sidecar type.
                    properties:
                      command:
                        description: Command specifies the container execution command
                          when using the "k8s" type.
                        items:
                          type: string
                        type: array
                      configMapRef:
                        description: ConfigMapRef references a ConfigMap containing
                          the container configuration when using the "custom" type.
                        properties:
                          name:
                            description: Name specifies the name of the referenced
                              ConfigMap.
                            type: string
                          namespace:
                            description: |-
                              Namespace allows referencing a ConfigMap from a different namespace.
                              This is by design to allow configuration generalization, where a single ConfigMap
                              can be shared and referenced by ConfigMapSets across multiple namespaces without duplication.
                              Security Implications & RBAC: The kruise-manager requires cluster-level 'get' permission
                              for ConfigMaps to resolve cross-namespace references. This permission is already included
                              in the default controller RBAC manifests.
                            type: string
                        type: object
                      image:
                        description: Image specifies the container image when using
                          the "k8s" type.
                        type: string
                      name:
                        description: Name specifies the container name when using
                          the "k8s" type.
                        type: string
                      restartPolicy:
                        description: RestartPolicy specifies the container restart
                          policy when using the "k8s" type.
                        type: string
                      sidecarSetRef:
                        description: SidecarSetRef references an OpenKruise SidecarSet
                          when using the "sidecarset" type.
                        properties:
                          containerName:
                            description: ContainerName is the name of the container
                              within the SidecarSet to be used.
                            type: string
                          name:
                            description: Name is the name of the SidecarSet.
                            type: string
                        type: object
                    type: object
                  type:
                    description: Type of the reload sidecar config. Can be "k8s" (native
                      pod template), "sidecarset" (injected via Kruise SidecarSet),
                      or "custom" (referenced via ConfigMap).
                    type: string
                type: object
              revisionHistoryLimit:
                description: |-
                  RevisionHistoryLimit indicates the maximum number of old configuration revisions
                  to retain for rollback purposes.
                format: int32
                type: integer
              selector:
                description: |-
                  Selector is a label query over Pods that should be managed by this ConfigMapSet.
                  Only Pods matching this selector will have their configurations updated.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              updateStrategy:
                description: |-
                  UpdateStrategy defines the rules for rolling out configuration updates to
                  the matched Pods, such as partition, maxUnavailable, and grouping.
                properties:
                  matchLabelKeys:
                    description: |-
                      MatchLabelKeys allows grouping matched Pods by label keys. The rollout strategy
                      (Partition/MaxUnavailable) will be applied independently within each group.
                    items:
                      type: string
                    type: array
                  maxUnavailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: MaxUnavailable is the maximum number of Pods that
                      can be unavailable during the update.
                    x-kubernetes-int-or-string: true
                  partition:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      Partition indicates the desired number or percentage of Pods in old revisions.
                      Pods beyond this partition will be updated to the new revision.
                    x-kubernetes-int-or-string: true
                type: object
            required:
            - selector
            type: object
          status:
            description: Status represents the current observed state of the ConfigMapSet.
            properties:
              currentCustomVersion:
                description: CurrentCustomVersion tracks the user-defined version
                  alias of the currently applied configuration.
                type: string
              currentRevision:
                description: CurrentRevision is the system-generated hash revision
                  of the currently applied configuration.
                type: string
              expectedUpdatedReplicas:
                description: ExpectedUpdatedReplicas is the total number of matched
                  Pods that are expected to be updated.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the ConfigMapSet controller.
                format: int64
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of matched Pods that have
                  a Ready Condition.
                format: int32
                type: integer
              replicas:
                description: Replicas is the total number of Pods that match the ConfigMapSet
                  selector.
                format: int32
                type: integer
              updateCustomVersion:
                description: UpdateCustomVersion tracks the user-defined version alias
                  of the configuration being rolled out.
                type: string
              updateRevision:
                description: UpdateRevision is the system-generated hash revision
                  of the configuration being rolled out.
                type: string
              updatedReadyReplicas:
                description: UpdatedReadyReplicas is the number of matched Pods that
                  have been updated to the latest revision and are Ready.
                format: int32
                type: integer
              updatedReplicas:
                description: UpdatedReplicas is the total number of matched Pods that
                  have been updated to the latest revision.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
